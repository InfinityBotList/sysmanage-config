[Unit]
Description=DB Backup Timer

[Timer]
OnBootSec=15min
Persistent=true
# OnUnitActiveSec=1h -- If monotonic is desired, maybe it is(?)
OnCalendar=hourly
Unit=ibl-dbbackup-timer.target

[Install]
WantedBy=timers.target
